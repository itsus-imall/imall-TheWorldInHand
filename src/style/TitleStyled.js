import styled from 'styled-components';

export const Wrapper = styled.div`
  /* flex:  */
  h2 {
    font-size: 1.25rem;
    line-height: 2;
    font-weight: 500;
  }
  p {
    font-size: var(--font-size-sm);
  }
`;
